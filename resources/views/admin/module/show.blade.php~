@extends('layouts.app')

@section('content')
<div class="container">

    <h1>Post</h1>
    <div class="table-responsive">
        <table class="table table-bordered table-striped table-hover">
            <thead>
                <tr>
                    <th>ID.</th> <th>{{ trans('posts.title') }}</th><th>{{ trans('posts.body') }}</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>{{ $post->id }}</td> <td> {{ $post->title }} </td><td> {{ $post->body }} </td>
                </tr>
            </tbody>
        </table>
    </div>

</div>
@endsection